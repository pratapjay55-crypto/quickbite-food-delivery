#include <stdio.h>
#include <string.h>
#include "stack.h"

/*
  NOTE: In the integrated version, stack state is fully
  managed inside queue.c using state.dat for persistence.
  push() and displayStack() are defined there to avoid
 duplicate symbol errors at link time.
 */